[project]
name = "codex"
version = "0.1.0"
dependencies = [
    "click",
    "PyYAML",
]

[project.scripts]
codex = "codex.cli:cli"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["codex*"]
